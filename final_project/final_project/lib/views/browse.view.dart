import 'package:flutter/material.dart';

class Browse extends StatelessWidget {
   Browse({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ini browse"))
    );
  }
}